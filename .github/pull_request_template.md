## What this changes

<!-- One or two sentences. For a bug fix, link the issue. -->

## Checklist

- [ ] Tests cover the change (for a fix: a test that failed before it)
- [ ] `python -m pytest` passes
- [ ] `CHANGELOG.md` has an entry under `## [Unreleased]`, written for the people who install it
- [ ] If it changes the CLI, `/api/v1`, ROS topics or file formats incompatibly, it is marked **Breaking** (see docs/RELEASING.md)
