# Files that run on the Pi must keep Unix line endings, even when the repo
# is checked out on Windows: a CRLF shell script or unit file fails there.
* text=auto
*.sh text eol=lf
*.service text eol=lf
*.env text eol=lf
*.pdf binary
*.png binary
*.ply binary
